<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/AppTheme.NoActionBar"
    tools:context=".activities.GroupChatActivity">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/group_chat_barLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:theme="@style/AppTheme.AppBarOverlay">

    <include
        android:id="@+id/group_chat_toolbar"
        layout="@layout/app_toolbar"
        >
    </include>

    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:id="@+id/groupChatScrollView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="3dp"
        android:layout_below="@+id/group_chat_barLayout"
        android:layout_above="@+id/newMessageLinearLayout"
        android:layout_alignParentStart="true"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <TextView
                android:id="@+id/msgDisplay"
                android:background="@drawable/rect_border_drawable"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="60dp"
                android:padding="4dp">
            </TextView>

        </LinearLayout>

    </ScrollView>

    <LinearLayout
        android:id="@+id/newMessageLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal" >

        <EditText
            android:id="@+id/messageEditText"
            android:layout_width="280dp"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:padding="4dp"
            android:inputType="text"
            android:hint="Enter your message..." />

        <ImageButton
            android:id="@+id/sendMsgButton"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            android:layout_alignRight="@+id/messageEditText"
            android:src="@drawable/email_icon"
            android:layout_gravity="center"
            >
        </ImageButton>

    </LinearLayout>

</RelativeLayout>